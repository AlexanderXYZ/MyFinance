<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_margin="6dp"
    app:cardBackgroundColor="@color/main_background_white"
    app:cardElevation="0dp"
    android:id="@+id/categories_card_item">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/category_item_cc"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="1dp">

        <ImageView
            android:id="@+id/icon_categories"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:background="@drawable/background_circle"
            android:scaleType="center"
            android:src="@drawable/ic_graphs"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <TextView
            android:id="@+id/title_categories_item"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="Family"
            app:layout_constraintEnd_toEndOf="@id/icon_categories"
            app:layout_constraintStart_toStartOf="@id/icon_categories"
            app:layout_constraintTop_toBottomOf="@id/icon_categories" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>